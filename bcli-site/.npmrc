# pnpm 10+/11+ refuses unknown postinstall build scripts. Allow the two
# Astro deps that legitimately need them (esbuild native binary, sharp
# image pipeline). Both are first-party Astro deps with trusted publishers.
only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
