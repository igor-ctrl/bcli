"""Device code auth flow for interactive CLI use (Phase 2 placeholder)."""

from __future__ import annotations


class DeviceCodeAuth:
    """Interactive device code flow — will be implemented in Phase 2."""

    def __init__(self, **kwargs) -> None:
        raise NotImplementedError(
            "Device code flow is not yet implemented. "
            "Use client_credentials auth method for now."
        )
